import React from 'react';

const About = () => (
  <React.Fragment>
    <h3>About us page</h3>
    <p>
      This is a contact app with CRUD operation. React core methods like :
      state, event, ES6 class based component, jsx and routing operation has
      been applied here. For optimizing jsx parcel bundler also integrated.
    </p>
  </React.Fragment>
);

export default About;
